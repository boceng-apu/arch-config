# sample hyprlock.conf
# for more configuration options, refer https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock
#
# rendered text in all widgets supports pango markup (e.g. <b> or <i> tags)
# ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#general-remarks
#
# shortcuts to clear password buffer: ESC, Ctrl+U, Ctrl+Backspace
#
# you can get started by copying this config to ~/.config/hypr/hyprlock.conf
# ~/.config/hypr/hyprlock.conf - 轻松惬意中文版
# ~/.config/hypr/hyprlock.conf - 深色文字版
background {
monitor =
path = screenshot
blur_passes = 4 # 增加模糊次数
blur_size = 12 # 增加模糊强度
noise = 0.02 # 减少噪点
contrast = 1.0 # 正常对比度
brightness = 0.9 # 稍微调暗
# 去掉任何颜色叠加，保持纯模糊效果
}
# 背景设置 - 柔焦效果（保持不变）
background {
    monitor =
    path = screenshot
    # path = /home/youth/wallpapers/wallpaper1.png
    blur_passes = 3    # 适度模糊
    blur_size = 8      # 柔和程度
    noise = 0.03       # 轻微噪点
    contrast = 0.95    # 柔和对比
    brightness = 1.02  # 稍暗背景
    # color =  rgba(44, 51, 71, 0.9)
}

# 用户头像 - 圆形设计
image {
    monitor =
    path = ~/.face
    size = 128
    rounding = -1      # 正圆形
    border_size = 3
    border_color = rgba(143, 188, 187, 0.8) # 薄荷绿边框
    position = 0, -180
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 5
    shadow_color = rgba(0, 0, 0, 0.15)
}

# 用户名显示 - 调整为深灰蓝色
label {
    monitor =
    text = <span font_weight="bold">你好，$USER</span>
    color = rgba(120, 135, 160, 0.9) # 调整为深灰蓝色
    font_size = 24
    font_family = "LXGW WenKai Screen"
    position = 0, -120
    halign = center
    valign = center
}

# 时间显示 - 主视觉焦点（调整为深色）
label {
    monitor =
    text = cmd[update:1000] echo "<span font_weight='bold'>$(date +"%H:%M")</span>"
    color = rgba(160, 170, 190, 0.95) # 调整为深灰蓝色
    font_size = 64
    font_family = "JetBrains Mono Nerd Font"
    position = 0, 0
    halign = center
    valign = center
}

# 日期显示 - 调整为更深的颜色
label {
    monitor =
    text = cmd[update:60000] echo "<span>$(LC_TIME=zh_CN.UTF-8 date +"%Y年%m月%d日 %A")</span>"
    color = rgba(140, 155, 175, 0.9) # 调整为更深的灰蓝色
    font_size = 18
    font_family = "LXGW WenKai Screen"
    position = 0, 70
    halign = center
    valign = center
}

# 密码输入框 - 调整文字颜色
input-field {
    monitor =
    size = 360, 56
    outline_thickness = 0
    dots_size = 0.3
    dots_spacing = 0.15
    rounding = 28       # 椭圆造型
    inner_color = rgba(76, 86, 106, 0.6) # 深灰蓝
    font_color = rgb(180, 185, 195) # 调整为更深的灰色
    font_family = "JetBrains Mono Nerd Font"
    placeholder_text = <i><span foreground="##76849a">请输入密码...</span></i> # 调整为深蓝色
    hide_input = false
    check_color = rgba(163, 190, 140, 1.0) # 抹茶绿
    fail_color = rgba(191, 97, 106, 1.0)   # 珊瑚红
    fail_text = <i><span foreground="##a23d48">$FAIL (尝试 $ATTEMPTS 次)</span></i> # 调整为深红色
    position = 0, 150
    halign = center
    valign = center
}

# 系统信息 - 调整为深色
label {
    monitor =
    text = cmd[update:10000] echo "<span>$(uname -n) · 运行 $(uptime -p | sed -e 's/up //' -e 's/days/天/' -e 's/hours/小时/' -e 's/minutes/分钟/')</span>"
    color = rgba(120, 135, 155, 0.8) # 调整为深灰蓝色
    font_size = 12
    font_family = "LXGW WenKai Screen"
    position = 20, -20
    halign = left
    valign = bottom
}

# 键盘布局 - 调整为深青绿色
label {
    monitor =
    text = <span>$LAYOUT[EN,中文]</span>
    color = rgba(120, 165, 160, 0.9) # 调整为深青绿色
    font_size = 14
    font_family = "LXGW WenKai Screen"
    position = -20, -20
    halign = right
    valign = bottom
}

# 指纹提示 - 调整为深青绿色
label {
    monitor =
    text = cmd[update:1000] if [ "$(systemctl is-active fprintd)" = "active" ]; then echo "<span font_size='14'>🖐️ 指纹识别就绪</span>"; fi
    color = rgba(120, 165, 160, 0.9) # 调整为深青绿色
    position = 0, 100
    halign = center
    valign = center
}
